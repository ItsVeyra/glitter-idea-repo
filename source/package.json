{
  "name": "glitter",
  "version": "0.1.10",
  "private": true,
  "description": "Minimal buildable source workspace for Obsidian community plugin review",
  "main": "dist/main.js",
  "scripts": {
    "build": "node esbuild.config.mjs production",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
